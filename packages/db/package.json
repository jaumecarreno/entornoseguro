{
  "name": "@entornoseguro/db",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@entornoseguro/shared": "0.1.0"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
